root = True

# Use a sane default for tab display at github
[*]
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true
indent_style = tab
indent_size = 4

# Might be good to keep trailing whitespace in markdown files
[*.md]
trim_trailing_whitespace = false
